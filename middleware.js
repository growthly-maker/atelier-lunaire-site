// Middleware temporairement désactivé pour diagnostic
// Pour réactiver, supprimez ce fichier et renommez middleware.js.backup en middleware.js

export { default } from 'next';

export const config = {
  matcher: [],
};